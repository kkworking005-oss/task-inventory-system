server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # Serve index.html for any route so React Router handles client-side routing
    location / {
        try_files $uri $uri/ /index.html;
    }
}
